[#${parent}-verify-deployment]
== Verifying the deployment

Confirm that the {project_name} deployment is ready.

[source,bash]
----
kubectl wait --for=condition=Ready passports.k8s.passport-pro.ai/passport
kubectl wait --for=condition=RollingUpdate=False passports.k8s.passport-pro.ai/passport
----
